function computeScore(l) { let s=0; if(l.stock_status==='in_stock') s+=50; if(l.seller_verified) s+=30; s+=Math.max(0,20-(l.shipping_days||20)); if(l.origin && l.origin.toLowerCase().includes('india')) s+=10; return s; }
async function run(input) { const listings = input.listings || []; return listings.map(l=>({ platform: l.platform||'Unknown', title: l.title||l.name||'Unknown product', price: l.price||null, currency: l.currency||'INR', shipping_days: l.shipping_days||7, link: l.link||'#', origin: l.origin||'Unknown', stock_status: l.stock_status||'unknown', seller_verified: !!l.seller_verified, availability_label: l.stock_status==='in_stock' ? '✅ Available' : (l.stock_status==='low_stock' ? '⚠️ Low Stock' : '❌ Out of Stock'), score: computeScore(l) })); }
module.exports = { run };
